<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Check_List_Less_than_Rep_Price__c</fullName>
    <description>Reporting Formula used to make sure List is more than Rep/RSM.</description>
    <externalId>false</externalId>
    <formula>OR(
AND(
Rep_Price__c != 0,
Rep_Price__c &gt; List_Price__c), 
AND(
RSM_Price__c != 0,
RSM_Price__c &gt; List_Price__c)
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Check List Less than Rep Price</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
